# LOC-FLOW-Notebook

LOC-FLOW Notebook is a Jupyter Notebook-based workflow framework for automated earthquake catalog construction from continuous waveform data. It reorganizes the original LOC-FLOW processing pipeline into an interactive and modular environment while maintaining the original earthquake detection and location algorithms.

The repository will provide two versions of LOC-FLOW Notebook:

## LOC-FLOW Notebook Lite

LOC-FLOW Notebook Lite is designed for small- to medium-scale earthquake datasets and routine research applications.

Features:
- Interactive Jupyter Notebook workflow
- Simplified user interface
- Standardized input and output management
- Suitable for rapid testing and routine earthquake catalog construction

## LOC-FLOW Notebook LScale

LOC-FLOW Notebook LScale is designed for large-scale earthquake sequence processing and high-volume continuous waveform datasets.

Features:
- Batch processing strategy for large earthquake catalogs
- Optimized data organization and task management
- Support for large numbers of seismic events and phase observations
- Designed for large-scale research applications

## User Version and Developer Version

Each version will include two distributions:

### User Version

The user version provides a simplified interface for routine applications. Core functions and classes are encapsulated to reduce unnecessary implementation complexity and allow users to focus on workflow execution and parameter configuration.

### Developer Version

The developer version provides the complete source structure, including internal functions, classes, and workflow modules. It is intended for users who want to modify, extend, or further develop LOC-FLOW Notebook.

## Availability

The full release of LOC-FLOW Notebook will be available after publication.
